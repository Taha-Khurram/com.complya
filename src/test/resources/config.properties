# Framework config (can be overridden via -Dkey=value)
browser=chrome
headless=true
baseUrl=https://complya.com/home/

# Timeouts
explicitWaitSeconds=15
pageLoadTimeoutSeconds=60

# TestNG retry
retry.count=2

