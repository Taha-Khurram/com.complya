# Framework config (can be overridden via -Dkey=value)
browser=chrome
headless=true
baseUrl=https://complya.com/app/sign-in

# Timeouts
explicitWaitSeconds=45
pageLoadTimeoutSeconds=60

# TestNG retry
retry.count=5

